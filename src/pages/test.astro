---
import Layout from "@layouts/Layout.astro";
import { testConn } from "@lib/server";

let queryRes;

try {
  queryRes = await testConn();
} catch (e) {
  queryRes = e;
}
---

<Layout title="test">
  <p>{JSON.stringify(queryRes)}</p>
</Layout>
